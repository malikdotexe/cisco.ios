---
# Molecule scenario: hostname
# Runs cisco.ios.ios_hostname integration tests against CISSHGO.
# Usage: cd cisco/ios/extensions && molecule test -s hostname

driver:
  name: default

platforms:
  - name: cisshgo-hostname-gathered
  - name: cisshgo-hostname-merged
  - name: cisshgo-hostname-deleted

ansible:
  executor:
    args:
      ansible_playbook:
        - --inventory=${MOLECULE_SCENARIO_DIRECTORY}/../cisshgo_fixtures/inventories/ansible/hostname.yaml
  env:
    ANSIBLE_FORCE_COLOR: "true"
    ANSIBLE_HOST_KEY_CHECKING: "false"
    ANSIBLE_DEPRECATION_WARNINGS: "false"

provisioner:
  name: ansible
  playbooks:
    create: ../_shared/create.yml
    converge: converge.yml
    verify: ../_shared/verify.yml
    destroy: ../_shared/destroy.yml

scenario:
  test_sequence:
    - create
    - converge
    - verify
    - destroy

prerun: false

verifier:
  name: ansible
...
